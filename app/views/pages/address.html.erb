<div class="container">
  <div class="main">
    <div class="center">
      <div class="headline">
        <h1>Sag uns, wo du arbeitest</h1>
      </div>
      <div class="text">
        <p>Zur Verifizierung benötigen wir den genauen Namen und die Adresse der Werkstatt, in der du arbeitest oder die du betreibst.</p>
        <br>
      </div>
      <form action="">
      <input class="form-control field" type="text" placeholder="Werkstattname">
      <div class="d-flex justify-content-between">
        <input class="form-control field wide" type="text" placeholder="Straße">
        <input class="form-control field short" type="number" placeholder="Nr.">
      </div>
      <div class="d-flex justify-content-between mb-3">
        <input class="form-control field short" type="number" placeholder="PLZ">
        <input class="form-control field wide" type="text" placeholder="Stadt">
      </div>
    </div>
  </div>
  <div class="bottom">
    <div class="buttons">
      <%= link_to "Zurück", password_path, class:"button-empty" %>
      <%= link_to "Weiter", foto_path, class:"button-blue small", id:"button-address" %>
    </div>
  </div>
  </form> 
</div>

<script>
  const buttonAddress = document.getElementById("button-address");
  buttonAddress.addEventListener("click", saveAddress);

  function saveAddress() {
    const array = document.querySelectorAll("input");
    let address = "";
    array.forEach(element => address = address + "," + element.value);
    console.log(address)
    sessionStorage.setItem("address", address);
  }
</script>